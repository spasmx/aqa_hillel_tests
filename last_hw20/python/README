1 . Пояснити різницю (кількома словами) між List та Tuple, які переваги у кожного типу?

Різниця між List та Tuple в тому, що List є змінюваним (mutable) типом даних, Tuple - не змінюваним (immutable)

Переваги List: можливість зміни даних, можливість додавання/видалення елементів, зручні методи для роботи з даним типом

Переваги Tuple: швидший доступ до елементів за допомогою індексу, не може бути змінений після створення,
може використовуватись як ключ у словниках та елемент у множинах.

2 . Що таке Set? Навести приклад коли варто застосовувати

Set - це неупорядкована колекція унікальних елементів.
Set варто використовувати:
Коли потрібно видалити дублікати елементів
Коли потрібно знайти унікальні елементі в колекції
Коли потрібно знайти спільні елементи між двома колекціями

9 . Пояснити що таке args та kwargs

*args - необов'язковий параметр функції, який дозволяє передавати довільку кількість аргументів у ф-цію.
Приймає аргументи у вигляді кортежу (tuple)

**kwargs - відрізняється від *args тим, що дозволяє пережавати довільну кількість іменованих аргументів у ф-цію.
Приймає аргументи у вигляді словника (dict)

Також ці параметри можна комбінувати


